const Message = ({ color }) => {
    return (
        <span 
            style={{ color: color, fontSize: "28px", fontWeight: "bold" }} 
        >
            1
        </span>
    );
}

export default Message;
